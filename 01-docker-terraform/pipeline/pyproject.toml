[project]
name = "pipeline"
version = "0.1.0"
description = "NYC Taxi data ingestion pipeline with DuckDB"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "click>=8.1.0",
    "duckdb>=1.1.3",
]

[dependency-groups]
dev = [
    "marimo>=0.19.7",
]
